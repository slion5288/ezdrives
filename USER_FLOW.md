# USER_FLOW.md — 用户操作流程图（唯一流程来源）

> 每条流程：入口明确 → 操作明确 → 成功/失败状态明确 → 下一步明确 → 返回路径明确。
> 与代码同步维护；后端校验失败会返回对应错误码，前端 toast 展示。

---

## A. 学员主线：注册 → 购买 → 预约 → 上课

### A1. 注册（新学员）
1. 入口：首页「课程预约」→ 选课程 → 「购买并预约」→ `/login?role=student&course=id`；或公开 `/student/book` 页头「登录」→ 切到注册。
2. 操作：姓名 + 手机号（含国家码）+ **邮箱（必填，通知渠道）** + 点「发送验证码」（Twilio 短信）+ 6 位验证码 + 密码（≥6 位）。
3. 成功：toast「欢迎 {姓名}！您的手机号已通过验证。重要账户、预约、日程与提醒通知将发送到 {邮箱}。」→ 自动进入 `/student/book`（带 course 参数则弹出支付）；系统同时向该邮箱发送欢迎邮件（best-effort，失败不影响注册）。
4. 失败：字段错误提示 / 验证码错误 / 手机号已注册 / **邮箱已注册**（后端校验，邮箱唯一）。
5. 返回：页脚「返回」→ `/`。

### A2. 登录（老学员）
1. 入口：同 A1。
2. 操作：手机号 + 密码。
3. 成功：→ `/student/book`。
4. 失败：提示「手机号或密码错误」。

### A3. 购买课程
1. 入口：`/student/book`（未登录先登录；已登录直接出现）。
2. 操作：点课程卡「购买并预约」→ 支付弹窗选方式（教练启用的方式）：
   - 现金/微信/e-Transfer/钱包：按提示当面/扫码/转账（e-Transfer 填参考号）；
   - 卡：填卡信息（本地校验，无真实扣款）。
3. 提交成功：toast「支付已提交，等待教练确认」→ 卡片显示「待教练确认」。
4. 教练确认后（30 秒轮询）：状态变「已确认」→ 可预约。
5. 失败：课程不存在/重复购买（后端拒绝）。

### A4. 预约时间
1. 入口：学员 Tab「预约时间」`/student`（或已购课程卡「继续预约」）。
2. 操作：选日期（绿色可约）→ 选开始时间 → 确认。
   - 套餐：课时列表按顺序选 1 个或 2 个连续课时，自动连约。
3. 成功：toast「已确认」；应用内通知双方（学员+教练）+ **邮件**（学员收 BOOKING_CONFIRMED，教练收 NEW_BOOKING，发到教练真实外部邮箱）。
4. 失败：时段冲突（「该时段已被预约」）/ 当天关闭 / 过去时间 / 未购买（后端校验）。
5. 改期/取消：个人中心或预约卡 → 确认弹窗；改期需重新选时间并过冲突校验；**改期/取消时学员与教练均收到相应邮件**（best-effort）。

### A5. 个人中心
- 入口：Tab「个人中心」`/student/profile`。
- 操作：填写接送地址（保存 → 后端落库）；导出 .ics / 复制订阅链接。
- 返回：Tab 切换 / logo → 首页。

### A6. 通知
- 入口：Tab「通知」（未读徽标）。
- 操作：点通知标已读 / 「全部已读」。
- 返回：Tab 切换。

---

## B. 教练主线：登录 → 后台 → 管理

### B1. 教练登录
1. 入口：首页页脚「教练工作台」→ `/login?role=instructor`。
2. 操作：**手机号或邮箱 + 密码**（当前临时密码 `123456`；无短信验证、无注册）。
3. 成功：→ `/instructor`（总览）。
4. 失败：「手机号或密码错误」。

### B2. 总览 → 查看即将到来的预约
- 入口：登录默认页；统计卡 + 近期预约面板。
- 操作：点预约 → 详情（电话 `tel:`/短信 `sms:`、地址 Google Maps 链接）。
- 返回：标签切换。

### B3. 日程管理（改期/取消/批量/导出）
1. 入口：标签「日程」。
2. 操作：周历看已约色块（学员姓名/电话/地址）→ 点预约 → 详情 → 改期（精确时间选择器，冲突检测）/ 取消（确认弹窗）；勾选多条约 → 批量移动到新时间；「导出 CSV」。
3. 成功：toast；学员收到应用内通知 + 邮件（改期/取消，best-effort）；空出的时段立即可约。
4. 失败：冲突 → 提示换时间。

### B4. 课程管理
- 入口：标签「课程」。
- 操作：新建/编辑/删除/上下架课程（中英双语、CAD 价格、封面图、套餐课时编辑）。
- 保存：`PUT /api/state` 全量落库；被预约引用的课程删除时自动改为下架。
- 返回：标签切换。

### B5. 学员管理
- 入口：标签「学员」。表格：姓名/电话/地址/邮箱/课程/课时/消费。

### B6. 支付确认
1. 入口：标签「支付」（或通知里点支付待确认）。
2. 操作：核对收款 → 确认（学员解锁预约，收到通知）/ 拒绝。
3. 返回：标签切换。

### B7. 设置（工作时间/车辆/视频/支付方式/收款）
1. 入口：标签「设置」。
2. 工作时间：周规则 + 单日例外（关闭/覆盖时段）+ 课间休息分钟数；保存后自动取消不再适合的预约并通知学员。
3. 车辆：增删改、照片、上下架。视频：YouTube 链接/本地上传、排序、上下架。
4. 支付方式：8 种开关（至少保留一种）→ 学员端实时跟随。
5. 收款设置：微信收款码、e-Transfer 邮箱、银行账户、Stripe/PayPal 凭证（暂不影响真实扣款）。
6. 返回：标签切换。

### B8. 通知（教练）
- 入口：标签「通知」。点通知标已读 / 「全部已读」。
- ⚠ 已知问题：后端 mark-read 只处理学员角色，教练已读**不生效**（见问题清单 P1）。

---

## C. 游客浏览

首页 → 浏览各锚点区块 → `/courses` 看全课程 → `/g1` 做题（无需登录）→ 想预约 → 课程卡 → 登录/注册（A 流程）。
返回：logo → 首页。

---

## D. 管理员主线：登录 → 内容/模板管理

### D1. 管理员登录
1. 入口：`/admin`。
2. 操作：用户名 + 密码（速率限制：5 次失败/5 分钟）。
3. 成功：→ 主页文字。
4. 失败：提示错误。

### D2. 通知模板管理（邮件系统，Change 17）
1. 入口：`/admin` → 标签「通知模板」。
2. 列表：14 个模板（名称/类型/主题/启用状态/编辑）。
3. 编辑：点「编辑」→ 预填当前值 → 修改主题/HTML/纯文本 → 「保存」（写库立即生效，无需重新部署）；「取消」放弃修改。
   - 安全类型（密码重置 / 重要账户）不可停用。
4. 预览：点「预览」→ 用样例数据（John Smith / TEST-001）渲染，未知 `{{变量}}` 高亮提示。
5. 测试：输入收件人邮箱 → 「发送测试邮件」→ 日志出现 sent/failed。
6. 日志：点「发送日志」→ 最近 50 条（类型/收件人/主题/状态/时间，失败显示原因）。
7. 状态栏：🟢 已连接（binding 就绪）或 🔴 未配置（提示查看 CLOUDFLARE_EMAIL_SETUP.md）。
